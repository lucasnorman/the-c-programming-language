#include <stdio.h>

/*
 * Bugs could be encountered in this program because it makes the false assumption that everytime
 * a user enters a space, newline, or tab they will start a new word. However, if these characters
 * are put back to back, the subsequent copies of them will be counted as new words.
 */

int main(void)
{
	return 0;
}